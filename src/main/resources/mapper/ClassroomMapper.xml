<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cueb.demo.classroom.mapper.ClassroomMapper">

    <select id="findAll" resultType="Classroom">
        SELECT id, name, building, capacity, create_time, update_time
        FROM classroom
        ORDER BY building, name
    </select>

    <select id="findById" resultType="Classroom">
        SELECT id, name, building, capacity, create_time, update_time
        FROM classroom
        WHERE id = #{id}
    </select>

    <select id="findByName" resultType="Classroom">
        SELECT id, name, building, capacity, create_time, update_time
        FROM classroom
        WHERE name = #{name}
        LIMIT 1
    </select>

</mapper>
